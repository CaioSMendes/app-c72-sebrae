<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.uhf"
    android:versionCode="1"
    android:versionName="1.0">

    <!-- Permissões -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-feature android:name="android.hardware.screen.portrait" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:theme="@style/CustomTheme">

        <!-- ✅ PROVIDER OBRIGATÓRIO (para compartilhar PDF/TXT no Android 7+) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <!-- ✅ FIM DO PROVIDER -->

        <activity
            android:name=".activity.HomeActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name="com.example.uhf.BootBroadcastReceiver"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </receiver>

        <activity android:name="com.example.uhf.filebrowser.FileManagerActivity"
            android:configChanges="orientation|screenSize"
            android:exported="true" />

        <activity android:name=".activity.InventarioActivity" />
        <activity android:name=".activity.ConsultaTagActivity" />
        <activity android:name=".activity.OpcaoActivity" />
        <activity android:name=".activity.InventarioCodBarraActivity" />
        <activity android:name=".activity.ConsultaCodBarraActivity" />

        <activity android:name=".activity.LocalActivity" />
        <activity android:name=".activity.ListaLocaisActivity" />
        <activity android:name=".activity.DetalheLocalActivity" />

        <activity android:name=".activity.PatrimonioActivity" />
        <activity android:name=".activity.ListaPatrimoniosActivity" />
        <activity android:name=".activity.DetalhePatrimonioActivity" />

        <activity android:name=".activity.UsuarioActivity" />
        <activity android:name=".activity.ListaUsuariosActivity" />
        <activity android:name=".activity.DetalheUsuarioActivity" />

        <activity android:name=".activity.ResumoActivity" />

        <activity android:name="com.example.uhf.activity.UHFMainActivity" />

    </application>

</manifest>
