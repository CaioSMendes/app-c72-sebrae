<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAFAFA">

    <!-- Logo -->
    <ImageView
        android:id="@+id/logo"
        android:layout_width="250dp"
        android:layout_height="90dp"
        android:src="@drawable/logo_sebrae"
        android:scaleType="fitCenter"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Linha 1 -->
    <LinearLayout
        android:id="@+id/row1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/logo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- LOCAL -->
        <LinearLayout
            android:id="@+id/btnLocal"
            android:layout_width="90dp"
            android:layout_height="100dp"
            android:layout_margin="6dp"
            android:background="@drawable/bg_btn_card"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="6dp">

            <ImageView
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@drawable/ic_locais"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:text="Local"
                android:textSize="12sp"
                android:textColor="#333"/>
        </LinearLayout>

        <!-- RESPONSÁVEL -->
        <LinearLayout
            android:id="@+id/btnUsuario"
            android:layout_width="90dp"
            android:layout_height="100dp"
            android:layout_margin="6dp"
            android:background="@drawable/bg_btn_card"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="6dp">

            <ImageView
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@drawable/ic_usuario"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:text="Responsável"
                android:textSize="12sp"
                android:textColor="#333"/>
        </LinearLayout>
    </LinearLayout>

    <!-- Linha 2 -->
    <LinearLayout
        android:id="@+id/row2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/row1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- INVENTÁRIO -->
        <LinearLayout
            android:id="@+id/btnInventario"
            android:layout_width="90dp"
            android:layout_height="100dp"
            android:layout_margin="6dp"
            android:background="@drawable/bg_btn_card"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="6dp">

            <ImageView
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@drawable/ic_inventario"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:text="Inventário"
                android:textSize="12sp"
                android:textColor="#333"/>
        </LinearLayout>

        <!-- PATRIMÔNIO -->
        <LinearLayout
            android:id="@+id/btnPatrimonio"
            android:layout_width="90dp"
            android:layout_height="100dp"
            android:layout_margin="6dp"
            android:background="@drawable/bg_btn_card"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="6dp">

            <ImageView
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@drawable/ic_ativo_pat"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:text="Patrimônio"
                android:textSize="12sp"
                android:textColor="#333"/>
        </LinearLayout>
    </LinearLayout>

    <!-- Texto inferior -->
    <TextView
        android:id="@+id/txtInfoSebrae"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Software de Inventário RFID destinado ao SEBRAE-Nacional©"
        android:textColor="#005EB8"
        android:textSize="12sp"
        android:gravity="center"
        android:padding="8dp"
        app:layout_constraintTop_toBottomOf="@id/row2"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Logo inferior -->
    <ImageView
        android:id="@+id/logoBottom"
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:layout_marginTop="4dp"
        android:src="@drawable/ic_brasilia_rfid"
        android:scaleType="fitCenter"
        app:layout_constraintTop_toBottomOf="@id/txtInfoSebrae"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
